<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
  <f:entry title="${%Build Result Message}" field="messages" >
    <f:repeatableProperty field="messages" default="${descriptor.getMessageList(instance.messages)}" />
  </f:entry>
</j:jelly>